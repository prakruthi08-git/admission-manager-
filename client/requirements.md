## Packages
recharts | Dashboard analytics charts and data visualization
lucide-react | Icons for the UI
react-hook-form | Form state management
@hookform/resolvers | Zod validation resolver for forms
zod | Schema validation

## Notes
Role management is simulated via a global React Context since there is no persistent auth backend required by the prompt.
All API hooks assume the endpoints return standard JSON and handle errors via standard HTTP status codes.
